EmberApp.QuestionsRoute = Ember.Route.extend({

	//model: function() {
		//return this.store.find('question', params.question_id);
	//	console.log(EmberApp.Question.find());
	//	return EmberApp.Question.find();
	//},

	//setupController: function() {
    	//this.controllerFor('questions').set('questions', EmberApp.Question.find());
  	//},
	//setupController: function(controller, model) {
    //	this.controllerFor('questions').set('questions', model);
  	//},

  		
	
	
});